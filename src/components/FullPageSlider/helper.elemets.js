// export function useMoveStyle(sizes, currentIndex, isHorizontal){
// 	if (isHorizontal) return {marginLeft: `${-(sizes.width) * currentIndex}px`}
// 	return {marginTop: `${-(sizes.height + 1) * currentIndex}px`}
// }